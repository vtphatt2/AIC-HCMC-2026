"""Phase 1: TransNetV2 scene-boundary detection and keyframe selection.

    decode   Frame decode (sequential + streaming) and keyframe selection
    model    TransNetV2 loading and GPU batch inference
    phase    Orchestration: sequential and streaming-global schedulers
"""
