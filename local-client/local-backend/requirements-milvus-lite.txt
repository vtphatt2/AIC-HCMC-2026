# Optional: lets SAMPLE mode query a lightweight, embedded, no-Docker Milvus
# instead of doing linear numpy search over locally loaded .npy files. Set
# MILVUS_LITE_PATH in .env to enable (see app/db/milvus_client.py). This is
# the local-backend workaround for remote-server's real Milvus — same
# app/db/milvus_client.py, same schema, lighter runtime.
pymilvus>=2.4
# Temporary pin until https://github.com/milvus-io/milvus-lite/pull/364 lands.
milvus-lite @ git+https://github.com/Le-Anh-Duy/milvus-lite.git@20951183b3c991ab11a0f776370bbe00b1b2dd19
