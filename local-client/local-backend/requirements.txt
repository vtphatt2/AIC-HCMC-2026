fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.10.6
numpy==2.2.6
tqdm==4.68.4

# Vietnamese -> English query paraphrasing uses the Gemini REST API through
# httpx. Set GEMINI_API_KEY; no extra SDK is required.

# PECore text encoder — "onnx" backend (PECORE_BACKEND=onnx, default here).
# CPU-only, no multi-GB model download. See app/services/simple_tokenizer.py
# and docs/archive/PE-Core-bigG-14-448-Text-Encoder.README.md.
onnxruntime==1.23.2
ftfy==6.3.1
regex==2026.7.10

# Fuzzy transcript search (app/services/transcript_index.py's
# Transcript.fuzzy_find / search_all_transcripts) — C-optimized partial-ratio
# matching, no embedding model needed.
rapidfuzz==3.14.3

# Bundled ffmpeg binary (no system-wide install needed) — used by both
# FRAME_IMAGE_SOURCE=local_video (app/services/local_video_frame.py) and the
# /api/zip-frame organizer-ZIP frame proxy (app/services/zip_frame_source.py).
imageio-ffmpeg>=0.4.9

# For the full "torch" backend (PECORE_BACKEND=torch, GPU-capable, downloads
# the full PE-Core weights), install requirements-torch.txt as well:
#   pip install -r requirements.txt -r requirements-torch.txt
