# Optional: FRAME_IMAGE_SOURCE=youtube_storyboard or youtube_precise workaround.
# See docs/youtube-storyboard-thumbnails-workaround.md
# Install alongside the base requirements.txt:
#   pip install -r requirements.txt -r requirements-youtube-thumbnail.txt
yt-dlp>=2024.1.0
pillow==12.3.0

# youtube_precise only: seeks into the real video stream with ffmpeg instead
# of cropping the low-res storyboard sprite. Bundles a static ffmpeg binary
# so no system-wide ffmpeg install is needed.
imageio-ffmpeg>=0.4.9
