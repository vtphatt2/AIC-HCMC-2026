fastapi==0.111.0
uvicorn[standard]==0.29.0
pymilvus==2.3.7
asyncpg==0.29.0
python-dotenv==1.0.1
httpx==0.27.0
pydantic==2.10.6
marshmallow==3.21.3
numpy==2.2.6
tqdm==4.68.4
sentence-transformers==5.6.0
transformers==4.57.6
torch==2.13.0
sentencepiece==0.2.2
huggingface-hub==0.36.0

# Vietnamese -> English translation via Google Translate's free web endpoint
# (see app/services/translation.py). No API key, low volume (~1-2 req/s).
deep-translator==1.11.4

# PECore text encoder — "onnx" backend (PECORE_BACKEND=onnx). CPU-only, no
# multi-GB model download. See app/services/simple_tokenizer.py and
# docs/archive/PE-Core-bigG-14-448-Text-Encoder.README.md.
# Note: sentence-transformers above already pulls in torch for transcript
# chunk embeddings, so this only saves the PE-Core-bigG weight download,
# not torch itself, on this server.
onnxruntime==1.23.2
ftfy==6.3.1
regex==2026.7.10
setuptools==81.0.0

# For the full "torch" backend (PECORE_BACKEND=torch, GPU-capable, downloads
# the full PE-Core weights), install requirements-torch.txt as well:
#   pip install -r requirements.txt -r requirements-torch.txt
