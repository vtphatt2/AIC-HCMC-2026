# Optional: full OpenCLIP "torch" backend (PECORE_BACKEND=torch).
# GPU-capable but downloads the multi-GB PE-Core-bigG-14-448 weights.
# Install alongside the base requirements.txt:
#   pip install -r requirements.txt -r requirements-torch.txt
open_clip_torch>=2.24.0
torch>=2.1.0
