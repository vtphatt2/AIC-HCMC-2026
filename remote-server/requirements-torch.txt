# Optional: "torch" text-tower backend (PECORE_BACKEND=torch). GPU-capable,
# but only builds the text tower from a local CACHED_MODEL directory — no
# weight download. safetensors comes transitively via transformers.
# Install alongside the base requirements.txt:
#   pip install -r requirements.txt -r requirements-torch.txt
open_clip_torch==3.3.0
torch==2.13.0
