# Optional: only needed for the "no Docker / no real Milvus server" local
# dev path (MILVUS_LITE_PATH in .env — see app/db/milvus_client.py). The
# base requirements.txt pins pymilvus==2.3.7 for the production real-Milvus
# path this server is meant to run in (remote-server carries the heavier
# workload — full Milvus, full dataset — local-backend's Milvus Lite is the
# lighter dev-only counterpart). Milvus Lite's local file/uri connection
# mode needs pymilvus>=2.4, which is why it's a separate opt-in install
# instead of bumping the base pin.
#
# Install alongside the base requirements.txt:
#   pip install -r requirements.txt -r requirements-milvus-lite.txt
pymilvus>=2.4
# Temporary pin until https://github.com/milvus-io/milvus-lite/pull/364 lands.
milvus-lite @ git+https://github.com/Le-Anh-Duy/milvus-lite.git@77aca8f6a3b406d3d327ab62db51077f79b1fdfc
