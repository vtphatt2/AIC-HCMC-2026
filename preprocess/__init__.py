"""ZIP-native video preprocessing plus transcript collection and cleaning.

The default ``python -m preprocess`` interface never extracts the full archive
and never materializes keyframe images. Transcript metadata ZIPs can be
collected and Gemini-cleaned concurrently with the video branch.
"""
